% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alea-select-light.R
\name{alea_select}
\alias{alea_select}
\alias{alea_select.numeric}
\alias{alea_select.alea_fit}
\title{AI-assisted distribution-selection support}
\usage{
alea_select(object, ...)

\method{alea_select}{numeric}(
  object,
  model = NULL,
  model_path = NULL,
  use_builtin_model = TRUE,
  sample_id = "observed_sample",
  param_id = "observed_sample",
  margin_thresholds = c(0.1, 0.3, 0.6),
  include_interpretation = TRUE,
  quiet = FALSE,
  ...
)

\method{alea_select}{alea_fit}(
  object,
  model = NULL,
  model_path = NULL,
  use_builtin_model = TRUE,
  sample_id = "observed_sample",
  param_id = "observed_sample",
  margin_thresholds = c(0.1, 0.3, 0.6),
  include_interpretation = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{object}{A numeric vector or an \code{alea_fit} object.}

\item{...}{Additional arguments passed to methods.}

\item{model}{Optional pre-loaded FADS_AI light model object.}

\item{model_path}{Optional path to a FADS_AI light model \code{.rds} file.}

\item{use_builtin_model}{Logical. If \code{TRUE}, use the bundled light model
when both \code{model} and \code{model_path} are \code{NULL}.}

\item{sample_id}{Character scalar identifying the sample.}

\item{param_id}{Character scalar identifying the FADS_AI application row.
The default is \code{"observed_sample"}.}

\item{margin_thresholds}{Numeric vector of three increasing thresholds used
to label the top1-top2 probability margin.}

\item{include_interpretation}{Logical. If \code{TRUE}, include report-ready
interpretation text in the output.}

\item{quiet}{Logical. If \code{FALSE}, a message is emitted when a model is
loaded from \code{model_path}.}
}
\value{
An object of class \code{alea_selection}.
}
\description{
\code{alea_select()} applies the lightweight operational FADS_AI model to a
hydrological sample and returns model-based support for the candidate
distribution families.
}
\details{
The default engine is the bundled lightweight FADS_AI application model,
stored under \code{inst/extdata/fads_ai/fads_ai_application_model_light.rds}.
The light model is an operational representation of the archived FADS_AI
XGBoost classifier and was validated to reproduce the full workflow
predictions on the validation check.

FADS_AI output should be interpreted as decision-support evidence for
candidate distribution families. It is not proof of the true generating
distribution and should not replace goodness-of-fit assessment, diagnostics,
return-level uncertainty evaluation, or hydrological judgement.
}
